var num = prompt("Por favor digite un numero");

document.write("<h2>Math.abs()</h2>");
document.write("<p>Retorna el valor absoluto de un número<br>Ejemplo aplicado:</p>");


document.write(Math.abs(num));

document.write("<h2>Math.round()</h2>");
document.write("<p>Retorna el valor de un número redondeado al entero más cercano.<br>Ejemplo aplicado:</p>");


document.write(Math.round(num));

document.write("<h2>Math.ceil()</h2>");
document.write("<p>Devuelve el entero mayor o igual más próximo a un número dado.<br>Ejemplo aplicado:</p>");


document.write(Math.ceil(num));


document.write("<h2>Math.floor()</h2>");
document.write("<p>Devuelve el máximo entero menor o igual a un número.<br>Ejemplo aplicado:</p>");


document.write(Math.floor(num));

document.write("<h2>Math.exp()</h2>");
document.write("<p>Devuelve ex, donde x es el argumento, y e es El número de Euler (también conocido como la constante de Napier), la base de los algoritmos naturales.<br>Ejemplo aplicado:</p>");


document.write(Math.exp(num));

document.write("<h2>Math.log()</h2>");
document.write("<p>Devuelve la base neutral de un número (base e)<br>Ejemplo aplicado:</p>");


document.write(Math.log(num));

document.write("<h2>Math.random()</h2>");
document.write("<p>Devuelve un número de coma flotante pseudo-aleatorio, comprendido en el rango de 0 a menor que 1 (es decir, incluido el 0 pero no el 1), con una distribución aproximadamente uniforme en este rango. <br>Ejemplo aplicado:</p>");


document.write(Math.random(num));
